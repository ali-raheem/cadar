import Text_IO;
use Text_IO;

type Buffer = [0..3] Integer;

fn Main() {
    Buffer Data = [1, 2, 3, 4];
    Data[1] = 9;
    Data[3] = Data[0] + Data[1];

    Put_Line(Integer.image(Data[0] + Data[1] + Data[2] + Data[3]));
    Put_Line(Integer.image(Data.length));
}
